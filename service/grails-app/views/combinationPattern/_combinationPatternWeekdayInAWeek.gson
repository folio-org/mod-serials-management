import groovy.transform.*
import org.olf.combination.combinationPattern.CombinationPatternWeekdayInAWeek
import groovy.transform.Field

def should_expand = [
  'weekday',
]

// Important that 'CombinationPatternWeekdayInAWeek' is named 'pattern' due to 'CombinationRule' template

@Field CombinationPatternWeekdayInAWeek pattern

json g.render(pattern, [excludes: ['owner'], expand: should_expand])
