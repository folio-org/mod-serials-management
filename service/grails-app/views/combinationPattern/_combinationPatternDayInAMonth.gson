import groovy.transform.*
import org.olf.combination.combinationPattern.CombinationPatternDayInAMonth
import groovy.transform.Field

def should_expand = [
  'month',
]

// Important that 'CombinationPatternDayInAMonth' is named 'pattern' due to 'CombinationRule' template

@Field CombinationPatternDayInAMonth pattern

json g.render(pattern, [excludes: ['owner'], expand: should_expand])
