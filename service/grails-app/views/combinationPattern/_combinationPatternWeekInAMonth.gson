import groovy.transform.*
import org.olf.combination.combinationPattern.CombinationPatternWeekInAMonth
import groovy.transform.Field

def should_expand = [
  'month'
]

// Important that 'CombinationPatternWeekInAMonth' is named 'pattern' due to 'CombinationRule' template

@Field CombinationPatternWeekInAMonth pattern

json g.render(pattern, [excludes: ['owner'], expand: should_expand])
