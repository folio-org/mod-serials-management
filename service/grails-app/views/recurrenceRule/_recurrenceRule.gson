import groovy.transform.*
import org.olf.recurrence.RecurrenceRule
import groovy.transform.Field

def should_expand = [
  'patternType',
]

@Field RecurrenceRule recurrenceRule
json g.render(recurrenceRule, [excludes: ['owner'], expand: should_expand])
