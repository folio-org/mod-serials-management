import groovy.transform.*
import org.olf.omission.omissionPattern.OmissionPatternWeekdaysInWeek
import groovy.transform.Field

def should_expand = [
  'weekday'
]

// Important that 'OmissionPatternWeekdaysInWeek' is named 'pattern' due to 'OmissionRule' template

@Field OmissionPatternWeekdaysInWeek pattern
json g.render(pattern, [excludes: ['owner'], expand: should_expand])
