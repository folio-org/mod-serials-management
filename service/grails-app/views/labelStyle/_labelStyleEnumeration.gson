import groovy.transform.*
import org.olf.label.labelStyle.LabelStyleEnumeration
import groovy.transform.Field

// Important that 'LabelStyleEnumeration' is named 'style' due to 'LabelRule' template

def should_expand = [
  'levels'
]

@Field LabelStyleEnumeration style

json g.render(style, [excludes: ['owner'], expand: should_expand])
