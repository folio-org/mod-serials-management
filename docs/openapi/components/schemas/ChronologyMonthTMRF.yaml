type: object
required:
  - monthFormat
  - yearFormat
properties: 
  id: 
    type: string
    format: uuid
  monthFormat:
    oneOf: 
      -  type: string
      - $ref: RefData.yaml
  yearFormat:
    oneOf:
      - type: string
      - $ref: RefData.yaml
